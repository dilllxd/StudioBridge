#!/bin/sh
set -e
if command -v udevadm >/dev/null 2>&1; then
  udevadm control --reload-rules || true
fi
printf '%s\n' 'Run: systemctl --user enable --now studiobridge.service'
printf '%s\n' 'General BEACN hardware writes remain disabled by default.'
exit 0
